# Tells Grafana to load dashboards from the mounted dashboards/ folder.
apiVersion: 1

providers:
  - name: autoroute
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
