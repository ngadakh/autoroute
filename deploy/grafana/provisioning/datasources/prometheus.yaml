# Auto-registers the compose Prometheus instance as Grafana's default
# datasource. Mounted read-only into /etc/grafana/provisioning/datasources.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus_ds
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
