apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "autoroute.fullname" . }}-catalogue
  labels:
    {{- include "autoroute.labels" . | nindent 4 }}
data:
  catalogue.yaml: |
{{ .Values.catalogue | indent 4 }}
